"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.hasScope = hasScope;
const combineScopes_ee_1 = require("./combineScopes.ee");
function hasScope(scope, userScopes, masks, options = { mode: 'oneOf' }) {
    if (!Array.isArray(scope)) {
        scope = [scope];
    }
    const userScopeSet = (0, combineScopes_ee_1.combineScopes)(userScopes, masks);
    if (options.mode === 'allOf') {
        return !!scope.length && scope.every((s) => userScopeSet.has(s));
    }
    return scope.some((s) => userScopeSet.has(s));
}
//# sourceMappingURL=hasScope.ee.js.map